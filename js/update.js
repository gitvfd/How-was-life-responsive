function update(year){
	GDPPerCapita(year);
 	realWages(year);
 	education(year);
 	lifeExpectancy(year);
 	heightFunction(year);
 	homicide(year);
 	polity(year);
 	co2(year);
 	so2(year);
 	cropland(year);
 	incomeIneq(year);
};